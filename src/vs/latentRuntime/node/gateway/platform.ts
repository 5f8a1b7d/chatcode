/* eslint-disable header/header */
/** The Gateway adapter contract is shared with runtime plugins (`latent.gatewayPlatforms`). */
export type { GatewayAdapterFactory, IGatewayAdapter, IGatewayAdapterContext, IInboundMessage } from '../../../platform/latentRuntime/common/runtimePlugin.js';
